name: Terraform CI

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:
  lint:
    name: "Lint"
    uses: opstimus/github-actions-terraform-lint@DEVOPS-101-tf-ci

